#!/usr/bin/env python3
# -*- coding: utf-8 -*-

# salle – Jenga Workspace
# Generated by `jenga workspace` on 2026-09-23 16:09:36
from Jenga import *

# ============================================================
# PROJET PRINCIPAL
# ============================================================

with project("MaSalle"):
    consoleapp()
    language("C++")
    cppdialect("C++17")
    location(".")
    files([
        "src/**.cpp",
        "include/**.hpp"
    ])

    # ========================================================
    # CHAPITRE 1 — Configuration de base
    # À ajouter : définition des configurations Debug/Release.
    # ========================================================
    # defines(["CHAPITRE_1"])

    # ========================================================
    # CHAPITRE 2 — Filtres
    # À ajouter : filtres selon le système d'exploitation.
    # ========================================================
    # with filter("system:Windows"):
    #     defines(["WINDOWS_CONFIG"])

    # ========================================================
    # CHAPITRE 3 — Dépendances
    # À ajouter : dépendances entre les différents modules.
    # ========================================================
    # dependson(["ModuleA"])

    # ========================================================
    # CHAPITRE 4 — Bibliothèques
    # À ajouter : bibliothèques spécifiques à chaque système.
    # ========================================================
    # with filter("system:Windows"):
    #     links(["user32"])
    # with filter("system:Linux"):
    #     links(["pthread"])

    # ========================================================
    # CHAPITRE 5 — Répertoires d'inclusion
    # À ajouter : répertoires supplémentaires pour les headers.
    # ========================================================
    # includedirs(["include", "Externals/include"])

    # ========================================================
    # CHAPITRE 6 — Définitions du préprocesseur
    # À ajouter : defines nécessaires aux différentes configurations.
    # ========================================================
    # defines(["MA_DEFINITION"])

    # ========================================================
    # CHAPITRE 7 — Optimisation
    # À ajouter : réglages différents pour Debug et Release.
    # ========================================================
    # with filter("config:Debug"):
    #     optimize("Off")
    # with filter("config:Release"):
    #     optimize("Speed")

    # ========================================================
    # CHAPITRE 8 — Symboles de débogage
    # À ajouter : activation des symboles en mode Debug.
    # ========================================================
    # with filter("config:Debug"):
    #     symbols(True)

    # ========================================================
    # CHAPITRE 9 — Compilation multiplateforme
    # À ajouter : configurations Windows, Linux et macOS.
    # ========================================================
    # with filter("system:Windows"):
    #     usetoolchain(TC_WINDOWS)
    # with filter("system:Linux"):
    #     usetoolchain("clang-native")

    # ========================================================
    # CHAPITRE 10 — Frameworks et bibliothèques système
    # À ajouter : frameworks ou bibliothèques propres à chaque OS.
    # ========================================================
    # with filter("system:Windows"):
    #     links(["user32"])
    # with filter("system:macOS"):
    #     frameworks(["Cocoa"])

    # ========================================================
    # CHAPITRE 11 — Organisation des fichiers générés
    # À ajouter : répertoires des objets et des exécutables.
    # ========================================================
    # objdir("%{wks.location}/Build/Obj/%{cfg.buildcfg}-%{cfg.system}/%{prj.name}")
    # targetdir("%{wks.location}/Build/Bin/%{cfg.buildcfg}-%{cfg.system}/%{prj.name}")

    # ========================================================
    # CHAPITRE 12 — Projet XR / moteur
    # À ajouter : dépendances du moteur XR et configuration
    # spécifique à la plateforme.
    # ========================================================
    # nkentseudependson(["NKXR", "NKRenderer"])

    # ========================================================
    # CHAPITRE 13 — Vulkan / RHI
    # À ajouter : définition Vulkan et headers nécessaires.
    # ========================================================
    # defines(["NK_RHI_VK_ENABLED"])
    # includedirs(["Externals/Libs/Vulkan-Headers/include"])

    # ========================================================
    # CHAPITRE 14 — Dépendances explicites du linker
    # À ajouter : bibliothèques qui ne sont pas récupérées
    # automatiquement par dépendance transitive.
    # ========================================================
    # nkentseudependson(["NKGLSlang", "NKSPIRVCross"])

    # ========================================================
    # CHAPITRE 15 — Configuration finale
    # À ajouter : réglages finaux de publication, version,
    # licence et informations de l'application.
    # ========================================================
    # apppublisher("Rihen Universe")
    # appversion("1.0.0")
    # licensefile("../../LICENSE")
